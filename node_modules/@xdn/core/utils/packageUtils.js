"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var path_1 = require("path");
var fs_extra_1 = require("fs-extra");
/**
 * Read in the contents of package.json
 * @param {String} [path=package.json] Relative path to the resouce
 * @return {Object}
 */
function read(path) {
    if (path === void 0) { path = 'package.json'; }
    return JSON.parse(fs_extra_1.readFileSync(path_1.join(process.cwd(), path), 'utf8'));
}
exports.read = read;
/**
 * Writes the contents to package.json
 * @param {String} contents
 * @param {String} [path='package.json'] Relative path to the resouce
 */
function write(contents, path) {
    if (path === void 0) { path = 'package.json'; }
    fs_extra_1.writeFileSync(path_1.join(process.cwd(), path), contents, 'utf8');
}
exports.write = write;
